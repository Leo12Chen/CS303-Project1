#include <iostream>
#include <cmath>
#include "Elevator.h"

using namepace std;

int main(){

struct Node{
int data;
Node*next;
Node*prev;
Node(const  int&data_item, Node*prev_val = NULL, DNode*next_val = Null): data(data_item),prev(prev_val),next(next_val){}
};

Node<int>*

return 0;
}
